/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI128Decoder,
  getI128Encoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_CONTEST_WINNER_DISCRIMINATOR = new Uint8Array([
  205, 219, 178, 214, 121, 1, 138, 112,
]);

export function getSetContestWinnerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_CONTEST_WINNER_DISCRIMINATOR
  );
}

export type SetContestWinnerInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountHost extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHost extends string
        ? WritableSignerAccount<TAccountHost> & AccountSignerMeta<TAccountHost>
        : TAccountHost,
      TAccountContest extends string
        ? WritableAccount<TAccountContest>
        : TAccountContest,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetContestWinnerInstructionData = {
  discriminator: ReadonlyUint8Array;
  winnerPubkey: Address;
  maxPnl: bigint;
};

export type SetContestWinnerInstructionDataArgs = {
  winnerPubkey: Address;
  maxPnl: number | bigint;
};

export function getSetContestWinnerInstructionDataEncoder(): FixedSizeEncoder<SetContestWinnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['winnerPubkey', getAddressEncoder()],
      ['maxPnl', getI128Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_CONTEST_WINNER_DISCRIMINATOR })
  );
}

export function getSetContestWinnerInstructionDataDecoder(): FixedSizeDecoder<SetContestWinnerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['winnerPubkey', getAddressDecoder()],
    ['maxPnl', getI128Decoder()],
  ]);
}

export function getSetContestWinnerInstructionDataCodec(): FixedSizeCodec<
  SetContestWinnerInstructionDataArgs,
  SetContestWinnerInstructionData
> {
  return combineCodec(
    getSetContestWinnerInstructionDataEncoder(),
    getSetContestWinnerInstructionDataDecoder()
  );
}

export type SetContestWinnerInput<
  TAccountHost extends string = string,
  TAccountContest extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  host: TransactionSigner<TAccountHost>;
  contest: Address<TAccountContest>;
  systemProgram?: Address<TAccountSystemProgram>;
  winnerPubkey: SetContestWinnerInstructionDataArgs['winnerPubkey'];
  maxPnl: SetContestWinnerInstructionDataArgs['maxPnl'];
};

export function getSetContestWinnerInstruction<
  TAccountHost extends string,
  TAccountContest extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: SetContestWinnerInput<
    TAccountHost,
    TAccountContest,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SetContestWinnerInstruction<
  TProgramAddress,
  TAccountHost,
  TAccountContest,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    host: { value: input.host ?? null, isWritable: true },
    contest: { value: input.contest ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.contest),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetContestWinnerInstructionDataEncoder().encode(
      args as SetContestWinnerInstructionDataArgs
    ),
    programAddress,
  } as SetContestWinnerInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountContest,
    TAccountSystemProgram
  >);
}

export type ParsedSetContestWinnerInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    host: TAccountMetas[0];
    contest: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: SetContestWinnerInstructionData;
};

export function parseSetContestWinnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetContestWinnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      host: getNextAccount(),
      contest: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetContestWinnerInstructionDataDecoder().decode(instruction.data),
  };
}
