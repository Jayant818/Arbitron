/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const STORE_CONTEST_INPUTS_DISCRIMINATOR = new Uint8Array([
  173, 99, 215, 107, 68, 197, 70, 109,
]);

export function getStoreContestInputsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    STORE_CONTEST_INPUTS_DISCRIMINATOR
  );
}

export type StoreContestInputsInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TAccountContestInputs extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountContest extends string
        ? ReadonlyAccount<TAccountContest>
        : TAccountContest,
      TAccountContestInputs extends string
        ? WritableAccount<TAccountContestInputs>
        : TAccountContestInputs,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type StoreContestInputsInstructionData = {
  discriminator: ReadonlyUint8Array;
  firstChunk: ReadonlyUint8Array;
};

export type StoreContestInputsInstructionDataArgs = {
  firstChunk: ReadonlyUint8Array;
};

export function getStoreContestInputsInstructionDataEncoder(): Encoder<StoreContestInputsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['firstChunk', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: STORE_CONTEST_INPUTS_DISCRIMINATOR })
  );
}

export function getStoreContestInputsInstructionDataDecoder(): Decoder<StoreContestInputsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['firstChunk', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getStoreContestInputsInstructionDataCodec(): Codec<
  StoreContestInputsInstructionDataArgs,
  StoreContestInputsInstructionData
> {
  return combineCodec(
    getStoreContestInputsInstructionDataEncoder(),
    getStoreContestInputsInstructionDataDecoder()
  );
}

export type StoreContestInputsAsyncInput<
  TAccountPayer extends string = string,
  TAccountContest extends string = string,
  TAccountContestInputs extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  contest: Address<TAccountContest>;
  contestInputs?: Address<TAccountContestInputs>;
  systemProgram?: Address<TAccountSystemProgram>;
  firstChunk: StoreContestInputsInstructionDataArgs['firstChunk'];
};

export async function getStoreContestInputsInstructionAsync<
  TAccountPayer extends string,
  TAccountContest extends string,
  TAccountContestInputs extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: StoreContestInputsAsyncInput<
    TAccountPayer,
    TAccountContest,
    TAccountContestInputs,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  StoreContestInputsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountContest,
    TAccountContestInputs,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    contest: { value: input.contest ?? null, isWritable: false },
    contestInputs: { value: input.contestInputs ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.contestInputs.value) {
    accounts.contestInputs.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            99, 111, 110, 116, 101, 115, 116, 95, 105, 110, 112, 117, 116, 115,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.contest.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.contest),
      getAccountMeta(accounts.contestInputs),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getStoreContestInputsInstructionDataEncoder().encode(
      args as StoreContestInputsInstructionDataArgs
    ),
    programAddress,
  } as StoreContestInputsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountContest,
    TAccountContestInputs,
    TAccountSystemProgram
  >);
}

export type StoreContestInputsInput<
  TAccountPayer extends string = string,
  TAccountContest extends string = string,
  TAccountContestInputs extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  contest: Address<TAccountContest>;
  contestInputs: Address<TAccountContestInputs>;
  systemProgram?: Address<TAccountSystemProgram>;
  firstChunk: StoreContestInputsInstructionDataArgs['firstChunk'];
};

export function getStoreContestInputsInstruction<
  TAccountPayer extends string,
  TAccountContest extends string,
  TAccountContestInputs extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: StoreContestInputsInput<
    TAccountPayer,
    TAccountContest,
    TAccountContestInputs,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): StoreContestInputsInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountContest,
  TAccountContestInputs,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    contest: { value: input.contest ?? null, isWritable: false },
    contestInputs: { value: input.contestInputs ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.contest),
      getAccountMeta(accounts.contestInputs),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getStoreContestInputsInstructionDataEncoder().encode(
      args as StoreContestInputsInstructionDataArgs
    ),
    programAddress,
  } as StoreContestInputsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountContest,
    TAccountContestInputs,
    TAccountSystemProgram
  >);
}

export type ParsedStoreContestInputsInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    contest: TAccountMetas[1];
    contestInputs: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: StoreContestInputsInstructionData;
};

export function parseStoreContestInputsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedStoreContestInputsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      contest: getNextAccount(),
      contestInputs: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getStoreContestInputsInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
