/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const APPEND_CONTEST_INPUTS_DISCRIMINATOR = new Uint8Array([
  150, 226, 116, 162, 252, 114, 23, 29,
]);

export function getAppendContestInputsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPEND_CONTEST_INPUTS_DISCRIMINATOR
  );
}

export type AppendContestInputsInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountContestInputs extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountContestInputs extends string
        ? WritableAccount<TAccountContestInputs>
        : TAccountContestInputs,
      TAccountContest extends string
        ? ReadonlyAccount<TAccountContest>
        : TAccountContest,
      ...TRemainingAccounts,
    ]
  >;

export type AppendContestInputsInstructionData = {
  discriminator: ReadonlyUint8Array;
  offset: number;
  chunk: ReadonlyUint8Array;
};

export type AppendContestInputsInstructionDataArgs = {
  offset: number;
  chunk: ReadonlyUint8Array;
};

export function getAppendContestInputsInstructionDataEncoder(): Encoder<AppendContestInputsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['offset', getU32Encoder()],
      ['chunk', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: APPEND_CONTEST_INPUTS_DISCRIMINATOR,
    })
  );
}

export function getAppendContestInputsInstructionDataDecoder(): Decoder<AppendContestInputsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['offset', getU32Decoder()],
    ['chunk', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getAppendContestInputsInstructionDataCodec(): Codec<
  AppendContestInputsInstructionDataArgs,
  AppendContestInputsInstructionData
> {
  return combineCodec(
    getAppendContestInputsInstructionDataEncoder(),
    getAppendContestInputsInstructionDataDecoder()
  );
}

export type AppendContestInputsAsyncInput<
  TAccountPayer extends string = string,
  TAccountContestInputs extends string = string,
  TAccountContest extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  contestInputs?: Address<TAccountContestInputs>;
  contest: Address<TAccountContest>;
  offset: AppendContestInputsInstructionDataArgs['offset'];
  chunk: AppendContestInputsInstructionDataArgs['chunk'];
};

export async function getAppendContestInputsInstructionAsync<
  TAccountPayer extends string,
  TAccountContestInputs extends string,
  TAccountContest extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: AppendContestInputsAsyncInput<
    TAccountPayer,
    TAccountContestInputs,
    TAccountContest
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  AppendContestInputsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountContestInputs,
    TAccountContest
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    contestInputs: { value: input.contestInputs ?? null, isWritable: true },
    contest: { value: input.contest ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.contestInputs.value) {
    accounts.contestInputs.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            99, 111, 110, 116, 101, 115, 116, 95, 105, 110, 112, 117, 116, 115,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.contest.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.contestInputs),
      getAccountMeta(accounts.contest),
    ],
    data: getAppendContestInputsInstructionDataEncoder().encode(
      args as AppendContestInputsInstructionDataArgs
    ),
    programAddress,
  } as AppendContestInputsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountContestInputs,
    TAccountContest
  >);
}

export type AppendContestInputsInput<
  TAccountPayer extends string = string,
  TAccountContestInputs extends string = string,
  TAccountContest extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  contestInputs: Address<TAccountContestInputs>;
  contest: Address<TAccountContest>;
  offset: AppendContestInputsInstructionDataArgs['offset'];
  chunk: AppendContestInputsInstructionDataArgs['chunk'];
};

export function getAppendContestInputsInstruction<
  TAccountPayer extends string,
  TAccountContestInputs extends string,
  TAccountContest extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: AppendContestInputsInput<
    TAccountPayer,
    TAccountContestInputs,
    TAccountContest
  >,
  config?: { programAddress?: TProgramAddress }
): AppendContestInputsInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountContestInputs,
  TAccountContest
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    contestInputs: { value: input.contestInputs ?? null, isWritable: true },
    contest: { value: input.contest ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.contestInputs),
      getAccountMeta(accounts.contest),
    ],
    data: getAppendContestInputsInstructionDataEncoder().encode(
      args as AppendContestInputsInstructionDataArgs
    ),
    programAddress,
  } as AppendContestInputsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountContestInputs,
    TAccountContest
  >);
}

export type ParsedAppendContestInputsInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    contestInputs: TAccountMetas[1];
    contest: TAccountMetas[2];
  };
  data: AppendContestInputsInstructionData;
};

export function parseAppendContestInputsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAppendContestInputsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      contestInputs: getNextAccount(),
      contest: getNextAccount(),
    },
    data: getAppendContestInputsInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
