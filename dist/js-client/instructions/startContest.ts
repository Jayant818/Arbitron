/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const START_CONTEST_DISCRIMINATOR = new Uint8Array([
  97, 146, 213, 7, 230, 233, 121, 237,
]);

export function getStartContestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    START_CONTEST_DISCRIMINATOR
  );
}

export type StartContestInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountHost extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHost extends string
        ? WritableSignerAccount<TAccountHost> & AccountSignerMeta<TAccountHost>
        : TAccountHost,
      TAccountContest extends string
        ? WritableAccount<TAccountContest>
        : TAccountContest,
      ...TRemainingAccounts,
    ]
  >;

export type StartContestInstructionData = { discriminator: ReadonlyUint8Array };

export type StartContestInstructionDataArgs = {};

export function getStartContestInstructionDataEncoder(): FixedSizeEncoder<StartContestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: START_CONTEST_DISCRIMINATOR })
  );
}

export function getStartContestInstructionDataDecoder(): FixedSizeDecoder<StartContestInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getStartContestInstructionDataCodec(): FixedSizeCodec<
  StartContestInstructionDataArgs,
  StartContestInstructionData
> {
  return combineCodec(
    getStartContestInstructionDataEncoder(),
    getStartContestInstructionDataDecoder()
  );
}

export type StartContestInput<
  TAccountHost extends string = string,
  TAccountContest extends string = string,
> = {
  host: TransactionSigner<TAccountHost>;
  contest: Address<TAccountContest>;
};

export function getStartContestInstruction<
  TAccountHost extends string,
  TAccountContest extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: StartContestInput<TAccountHost, TAccountContest>,
  config?: { programAddress?: TProgramAddress }
): StartContestInstruction<TProgramAddress, TAccountHost, TAccountContest> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    host: { value: input.host ?? null, isWritable: true },
    contest: { value: input.contest ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.host), getAccountMeta(accounts.contest)],
    data: getStartContestInstructionDataEncoder().encode({}),
    programAddress,
  } as StartContestInstruction<TProgramAddress, TAccountHost, TAccountContest>);
}

export type ParsedStartContestInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    host: TAccountMetas[0];
    contest: TAccountMetas[1];
  };
  data: StartContestInstructionData;
};

export function parseStartContestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedStartContestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { host: getNextAccount(), contest: getNextAccount() },
    data: getStartContestInstructionDataDecoder().decode(instruction.data),
  };
}
