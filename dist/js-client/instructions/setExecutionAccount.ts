/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_EXECUTION_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  15, 98, 193, 203, 135, 237, 20, 157,
]);

export function getSetExecutionAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_EXECUTION_ACCOUNT_DISCRIMINATOR
  );
}

export type SetExecutionAccountInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountHost extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHost extends string
        ? WritableSignerAccount<TAccountHost> & AccountSignerMeta<TAccountHost>
        : TAccountHost,
      TAccountContest extends string
        ? WritableAccount<TAccountContest>
        : TAccountContest,
      ...TRemainingAccounts,
    ]
  >;

export type SetExecutionAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  executionAccount: Address;
};

export type SetExecutionAccountInstructionDataArgs = {
  executionAccount: Address;
};

export function getSetExecutionAccountInstructionDataEncoder(): FixedSizeEncoder<SetExecutionAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['executionAccount', getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_EXECUTION_ACCOUNT_DISCRIMINATOR,
    })
  );
}

export function getSetExecutionAccountInstructionDataDecoder(): FixedSizeDecoder<SetExecutionAccountInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['executionAccount', getAddressDecoder()],
  ]);
}

export function getSetExecutionAccountInstructionDataCodec(): FixedSizeCodec<
  SetExecutionAccountInstructionDataArgs,
  SetExecutionAccountInstructionData
> {
  return combineCodec(
    getSetExecutionAccountInstructionDataEncoder(),
    getSetExecutionAccountInstructionDataDecoder()
  );
}

export type SetExecutionAccountInput<
  TAccountHost extends string = string,
  TAccountContest extends string = string,
> = {
  host: TransactionSigner<TAccountHost>;
  contest: Address<TAccountContest>;
  executionAccount: SetExecutionAccountInstructionDataArgs['executionAccount'];
};

export function getSetExecutionAccountInstruction<
  TAccountHost extends string,
  TAccountContest extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: SetExecutionAccountInput<TAccountHost, TAccountContest>,
  config?: { programAddress?: TProgramAddress }
): SetExecutionAccountInstruction<
  TProgramAddress,
  TAccountHost,
  TAccountContest
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    host: { value: input.host ?? null, isWritable: true },
    contest: { value: input.contest ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.host), getAccountMeta(accounts.contest)],
    data: getSetExecutionAccountInstructionDataEncoder().encode(
      args as SetExecutionAccountInstructionDataArgs
    ),
    programAddress,
  } as SetExecutionAccountInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountContest
  >);
}

export type ParsedSetExecutionAccountInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    host: TAccountMetas[0];
    contest: TAccountMetas[1];
  };
  data: SetExecutionAccountInstructionData;
};

export function parseSetExecutionAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetExecutionAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { host: getNextAccount(), contest: getNextAccount() },
    data: getSetExecutionAccountInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
