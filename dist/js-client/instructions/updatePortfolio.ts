/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getSelectedTokenDecoder,
  getSelectedTokenEncoder,
  type SelectedToken,
  type SelectedTokenArgs,
} from '../types';

export const UPDATE_PORTFOLIO_DISCRIMINATOR = new Uint8Array([
  0, 33, 180, 193, 28, 56, 215, 4,
]);

export function getUpdatePortfolioDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_PORTFOLIO_DISCRIMINATOR
  );
}

export type UpdatePortfolioInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountHost extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TAccountParticipentPortfolio extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountHost extends string
        ? ReadonlyAccount<TAccountHost>
        : TAccountHost,
      TAccountContest extends string
        ? ReadonlyAccount<TAccountContest>
        : TAccountContest,
      TAccountParticipentPortfolio extends string
        ? WritableAccount<TAccountParticipentPortfolio>
        : TAccountParticipentPortfolio,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdatePortfolioInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenSelected: Array<SelectedToken>;
};

export type UpdatePortfolioInstructionDataArgs = {
  tokenSelected: Array<SelectedTokenArgs>;
};

export function getUpdatePortfolioInstructionDataEncoder(): Encoder<UpdatePortfolioInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['tokenSelected', getArrayEncoder(getSelectedTokenEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_PORTFOLIO_DISCRIMINATOR })
  );
}

export function getUpdatePortfolioInstructionDataDecoder(): Decoder<UpdatePortfolioInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['tokenSelected', getArrayDecoder(getSelectedTokenDecoder())],
  ]);
}

export function getUpdatePortfolioInstructionDataCodec(): Codec<
  UpdatePortfolioInstructionDataArgs,
  UpdatePortfolioInstructionData
> {
  return combineCodec(
    getUpdatePortfolioInstructionDataEncoder(),
    getUpdatePortfolioInstructionDataDecoder()
  );
}

export type UpdatePortfolioAsyncInput<
  TAccountSigner extends string = string,
  TAccountHost extends string = string,
  TAccountContest extends string = string,
  TAccountParticipentPortfolio extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  host: Address<TAccountHost>;
  contest: Address<TAccountContest>;
  participentPortfolio?: Address<TAccountParticipentPortfolio>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenSelected: UpdatePortfolioInstructionDataArgs['tokenSelected'];
};

export async function getUpdatePortfolioInstructionAsync<
  TAccountSigner extends string,
  TAccountHost extends string,
  TAccountContest extends string,
  TAccountParticipentPortfolio extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: UpdatePortfolioAsyncInput<
    TAccountSigner,
    TAccountHost,
    TAccountContest,
    TAccountParticipentPortfolio,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdatePortfolioInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountHost,
    TAccountContest,
    TAccountParticipentPortfolio,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    host: { value: input.host ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: false },
    participentPortfolio: {
      value: input.participentPortfolio ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.participentPortfolio.value) {
    accounts.participentPortfolio.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 114, 116, 102, 111, 108, 105, 111])
        ),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
        getAddressEncoder().encode(expectAddress(accounts.contest.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.contest),
      getAccountMeta(accounts.participentPortfolio),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUpdatePortfolioInstructionDataEncoder().encode(
      args as UpdatePortfolioInstructionDataArgs
    ),
    programAddress,
  } as UpdatePortfolioInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountHost,
    TAccountContest,
    TAccountParticipentPortfolio,
    TAccountSystemProgram
  >);
}

export type UpdatePortfolioInput<
  TAccountSigner extends string = string,
  TAccountHost extends string = string,
  TAccountContest extends string = string,
  TAccountParticipentPortfolio extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  host: Address<TAccountHost>;
  contest: Address<TAccountContest>;
  participentPortfolio: Address<TAccountParticipentPortfolio>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenSelected: UpdatePortfolioInstructionDataArgs['tokenSelected'];
};

export function getUpdatePortfolioInstruction<
  TAccountSigner extends string,
  TAccountHost extends string,
  TAccountContest extends string,
  TAccountParticipentPortfolio extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: UpdatePortfolioInput<
    TAccountSigner,
    TAccountHost,
    TAccountContest,
    TAccountParticipentPortfolio,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UpdatePortfolioInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountHost,
  TAccountContest,
  TAccountParticipentPortfolio,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    host: { value: input.host ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: false },
    participentPortfolio: {
      value: input.participentPortfolio ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.contest),
      getAccountMeta(accounts.participentPortfolio),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUpdatePortfolioInstructionDataEncoder().encode(
      args as UpdatePortfolioInstructionDataArgs
    ),
    programAddress,
  } as UpdatePortfolioInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountHost,
    TAccountContest,
    TAccountParticipentPortfolio,
    TAccountSystemProgram
  >);
}

export type ParsedUpdatePortfolioInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    host: TAccountMetas[1];
    contest: TAccountMetas[2];
    participentPortfolio: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: UpdatePortfolioInstructionData;
};

export function parseUpdatePortfolioInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdatePortfolioInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      host: getNextAccount(),
      contest: getNextAccount(),
      participentPortfolio: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdatePortfolioInstructionDataDecoder().decode(instruction.data),
  };
}
