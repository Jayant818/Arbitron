/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getSelectedTokenDecoder,
  getSelectedTokenEncoder,
  type SelectedToken,
  type SelectedTokenArgs,
} from '../types';

export const CREATE_PORTFOLIO_DISCRIMINATOR = new Uint8Array([
  54, 26, 137, 113, 59, 182, 53, 225,
]);

export function getCreatePortfolioDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_PORTFOLIO_DISCRIMINATOR
  );
}

export type CreatePortfolioInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountHost extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TAccountParticipantPortfolio extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountHost extends string
        ? ReadonlyAccount<TAccountHost>
        : TAccountHost,
      TAccountContest extends string
        ? ReadonlyAccount<TAccountContest>
        : TAccountContest,
      TAccountParticipantPortfolio extends string
        ? WritableAccount<TAccountParticipantPortfolio>
        : TAccountParticipantPortfolio,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreatePortfolioInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenSelected: Array<SelectedToken>;
};

export type CreatePortfolioInstructionDataArgs = {
  tokenSelected: Array<SelectedTokenArgs>;
};

export function getCreatePortfolioInstructionDataEncoder(): Encoder<CreatePortfolioInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['tokenSelected', getArrayEncoder(getSelectedTokenEncoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_PORTFOLIO_DISCRIMINATOR })
  );
}

export function getCreatePortfolioInstructionDataDecoder(): Decoder<CreatePortfolioInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['tokenSelected', getArrayDecoder(getSelectedTokenDecoder())],
  ]);
}

export function getCreatePortfolioInstructionDataCodec(): Codec<
  CreatePortfolioInstructionDataArgs,
  CreatePortfolioInstructionData
> {
  return combineCodec(
    getCreatePortfolioInstructionDataEncoder(),
    getCreatePortfolioInstructionDataDecoder()
  );
}

export type CreatePortfolioAsyncInput<
  TAccountUser extends string = string,
  TAccountHost extends string = string,
  TAccountContest extends string = string,
  TAccountParticipantPortfolio extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  host: Address<TAccountHost>;
  contest: Address<TAccountContest>;
  participantPortfolio?: Address<TAccountParticipantPortfolio>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenSelected: CreatePortfolioInstructionDataArgs['tokenSelected'];
};

export async function getCreatePortfolioInstructionAsync<
  TAccountUser extends string,
  TAccountHost extends string,
  TAccountContest extends string,
  TAccountParticipantPortfolio extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: CreatePortfolioAsyncInput<
    TAccountUser,
    TAccountHost,
    TAccountContest,
    TAccountParticipantPortfolio,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreatePortfolioInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountHost,
    TAccountContest,
    TAccountParticipantPortfolio,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    host: { value: input.host ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: false },
    participantPortfolio: {
      value: input.participantPortfolio ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.participantPortfolio.value) {
    accounts.participantPortfolio.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 114, 116, 102, 111, 108, 105, 111])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
        getAddressEncoder().encode(expectAddress(accounts.contest.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.contest),
      getAccountMeta(accounts.participantPortfolio),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePortfolioInstructionDataEncoder().encode(
      args as CreatePortfolioInstructionDataArgs
    ),
    programAddress,
  } as CreatePortfolioInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountHost,
    TAccountContest,
    TAccountParticipantPortfolio,
    TAccountSystemProgram
  >);
}

export type CreatePortfolioInput<
  TAccountUser extends string = string,
  TAccountHost extends string = string,
  TAccountContest extends string = string,
  TAccountParticipantPortfolio extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  host: Address<TAccountHost>;
  contest: Address<TAccountContest>;
  participantPortfolio: Address<TAccountParticipantPortfolio>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenSelected: CreatePortfolioInstructionDataArgs['tokenSelected'];
};

export function getCreatePortfolioInstruction<
  TAccountUser extends string,
  TAccountHost extends string,
  TAccountContest extends string,
  TAccountParticipantPortfolio extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: CreatePortfolioInput<
    TAccountUser,
    TAccountHost,
    TAccountContest,
    TAccountParticipantPortfolio,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreatePortfolioInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountHost,
  TAccountContest,
  TAccountParticipantPortfolio,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    host: { value: input.host ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: false },
    participantPortfolio: {
      value: input.participantPortfolio ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.contest),
      getAccountMeta(accounts.participantPortfolio),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePortfolioInstructionDataEncoder().encode(
      args as CreatePortfolioInstructionDataArgs
    ),
    programAddress,
  } as CreatePortfolioInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountHost,
    TAccountContest,
    TAccountParticipantPortfolio,
    TAccountSystemProgram
  >);
}

export type ParsedCreatePortfolioInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    host: TAccountMetas[1];
    contest: TAccountMetas[2];
    participantPortfolio: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreatePortfolioInstructionData;
};

export function parseCreatePortfolioInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreatePortfolioInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      host: getNextAccount(),
      contest: getNextAccount(),
      participantPortfolio: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatePortfolioInstructionDataDecoder().decode(instruction.data),
  };
}
