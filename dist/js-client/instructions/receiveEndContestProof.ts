/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { ARBITRON_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const RECEIVE_END_CONTEST_PROOF_DISCRIMINATOR = new Uint8Array([
  59, 177, 66, 179, 71, 128, 115, 196,
]);

export function getReceiveEndContestProofDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECEIVE_END_CONTEST_PROOF_DISCRIMINATOR
  );
}

export type ReceiveEndContestProofInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountExecutionRequest extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountExecutionRequest extends string
        ? ReadonlyAccount<TAccountExecutionRequest>
        : TAccountExecutionRequest,
      TAccountContest extends string
        ? WritableAccount<TAccountContest>
        : TAccountContest,
      ...TRemainingAccounts,
    ]
  >;

export type ReceiveEndContestProofInstructionData = {
  discriminator: ReadonlyUint8Array;
  data: ReadonlyUint8Array;
};

export type ReceiveEndContestProofInstructionDataArgs = {
  data: ReadonlyUint8Array;
};

export function getReceiveEndContestProofInstructionDataEncoder(): Encoder<ReceiveEndContestProofInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['data', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: RECEIVE_END_CONTEST_PROOF_DISCRIMINATOR,
    })
  );
}

export function getReceiveEndContestProofInstructionDataDecoder(): Decoder<ReceiveEndContestProofInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['data', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getReceiveEndContestProofInstructionDataCodec(): Codec<
  ReceiveEndContestProofInstructionDataArgs,
  ReceiveEndContestProofInstructionData
> {
  return combineCodec(
    getReceiveEndContestProofInstructionDataEncoder(),
    getReceiveEndContestProofInstructionDataDecoder()
  );
}

export type ReceiveEndContestProofInput<
  TAccountExecutionRequest extends string = string,
  TAccountContest extends string = string,
> = {
  executionRequest: Address<TAccountExecutionRequest>;
  contest: Address<TAccountContest>;
  data: ReceiveEndContestProofInstructionDataArgs['data'];
};

export function getReceiveEndContestProofInstruction<
  TAccountExecutionRequest extends string,
  TAccountContest extends string,
  TProgramAddress extends Address = typeof ARBITRON_PROGRAM_ADDRESS,
>(
  input: ReceiveEndContestProofInput<TAccountExecutionRequest, TAccountContest>,
  config?: { programAddress?: TProgramAddress }
): ReceiveEndContestProofInstruction<
  TProgramAddress,
  TAccountExecutionRequest,
  TAccountContest
> {
  // Program address.
  const programAddress = config?.programAddress ?? ARBITRON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    executionRequest: {
      value: input.executionRequest ?? null,
      isWritable: false,
    },
    contest: { value: input.contest ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.executionRequest),
      getAccountMeta(accounts.contest),
    ],
    data: getReceiveEndContestProofInstructionDataEncoder().encode(
      args as ReceiveEndContestProofInstructionDataArgs
    ),
    programAddress,
  } as ReceiveEndContestProofInstruction<
    TProgramAddress,
    TAccountExecutionRequest,
    TAccountContest
  >);
}

export type ParsedReceiveEndContestProofInstruction<
  TProgram extends string = typeof ARBITRON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    executionRequest: TAccountMetas[0];
    contest: TAccountMetas[1];
  };
  data: ReceiveEndContestProofInstructionData;
};

export function parseReceiveEndContestProofInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedReceiveEndContestProofInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { executionRequest: getNextAccount(), contest: getNextAccount() },
    data: getReceiveEndContestProofInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
