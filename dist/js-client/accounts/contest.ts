/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI128Decoder,
  getI128Encoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getContestStateDecoder,
  getContestStateEncoder,
  type ContestState,
  type ContestStateArgs,
} from '../types';

export const CONTEST_DISCRIMINATOR = new Uint8Array([
  216, 26, 88, 18, 251, 80, 201, 96,
]);

export function getContestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CONTEST_DISCRIMINATOR);
}

export type Contest = {
  discriminator: ReadonlyUint8Array;
  name: string;
  duration: bigint;
  startTime: bigint;
  host: Address;
  entryFees: bigint;
  maxParticipents: number;
  participentsCount: number;
  status: ContestState;
  bump: number;
  prizePoolVaultUsdc: Address;
  currentExecutionAccount: Option<Address>;
  winner: Option<Address>;
  winnerPnl: Option<bigint>;
  isPrizeClaimed: boolean;
};

export type ContestArgs = {
  name: string;
  duration: number | bigint;
  startTime: number | bigint;
  host: Address;
  entryFees: number | bigint;
  maxParticipents: number;
  participentsCount: number;
  status: ContestStateArgs;
  bump: number;
  prizePoolVaultUsdc: Address;
  currentExecutionAccount: OptionOrNullable<Address>;
  winner: OptionOrNullable<Address>;
  winnerPnl: OptionOrNullable<number | bigint>;
  isPrizeClaimed: boolean;
};

export function getContestEncoder(): Encoder<ContestArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['duration', getU64Encoder()],
      ['startTime', getI64Encoder()],
      ['host', getAddressEncoder()],
      ['entryFees', getU64Encoder()],
      ['maxParticipents', getU32Encoder()],
      ['participentsCount', getU32Encoder()],
      ['status', getContestStateEncoder()],
      ['bump', getU8Encoder()],
      ['prizePoolVaultUsdc', getAddressEncoder()],
      ['currentExecutionAccount', getOptionEncoder(getAddressEncoder())],
      ['winner', getOptionEncoder(getAddressEncoder())],
      ['winnerPnl', getOptionEncoder(getI128Encoder())],
      ['isPrizeClaimed', getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CONTEST_DISCRIMINATOR })
  );
}

export function getContestDecoder(): Decoder<Contest> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['duration', getU64Decoder()],
    ['startTime', getI64Decoder()],
    ['host', getAddressDecoder()],
    ['entryFees', getU64Decoder()],
    ['maxParticipents', getU32Decoder()],
    ['participentsCount', getU32Decoder()],
    ['status', getContestStateDecoder()],
    ['bump', getU8Decoder()],
    ['prizePoolVaultUsdc', getAddressDecoder()],
    ['currentExecutionAccount', getOptionDecoder(getAddressDecoder())],
    ['winner', getOptionDecoder(getAddressDecoder())],
    ['winnerPnl', getOptionDecoder(getI128Decoder())],
    ['isPrizeClaimed', getBooleanDecoder()],
  ]);
}

export function getContestCodec(): Codec<ContestArgs, Contest> {
  return combineCodec(getContestEncoder(), getContestDecoder());
}

export function decodeContest<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Contest, TAddress>;
export function decodeContest<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Contest, TAddress>;
export function decodeContest<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Contest, TAddress> | MaybeAccount<Contest, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getContestDecoder()
  );
}

export async function fetchContest<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Contest, TAddress>> {
  const maybeAccount = await fetchMaybeContest(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeContest<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Contest, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeContest(maybeAccount);
}

export async function fetchAllContest(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Contest>[]> {
  const maybeAccounts = await fetchAllMaybeContest(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeContest(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Contest>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeContest(maybeAccount));
}
