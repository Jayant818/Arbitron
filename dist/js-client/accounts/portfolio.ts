/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getSelectedTokenDecoder,
  getSelectedTokenEncoder,
  type SelectedToken,
  type SelectedTokenArgs,
} from '../types';

export const PORTFOLIO_DISCRIMINATOR = new Uint8Array([
  94, 158, 71, 245, 122, 102, 110, 225,
]);

export function getPortfolioDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PORTFOLIO_DISCRIMINATOR);
}

export type Portfolio = {
  discriminator: ReadonlyUint8Array;
  contest: Address;
  user: Address;
  tokenSelected: Array<SelectedToken>;
  bump: number;
};

export type PortfolioArgs = {
  contest: Address;
  user: Address;
  tokenSelected: Array<SelectedTokenArgs>;
  bump: number;
};

export function getPortfolioEncoder(): Encoder<PortfolioArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['contest', getAddressEncoder()],
      ['user', getAddressEncoder()],
      ['tokenSelected', getArrayEncoder(getSelectedTokenEncoder())],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PORTFOLIO_DISCRIMINATOR })
  );
}

export function getPortfolioDecoder(): Decoder<Portfolio> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['contest', getAddressDecoder()],
    ['user', getAddressDecoder()],
    ['tokenSelected', getArrayDecoder(getSelectedTokenDecoder())],
    ['bump', getU8Decoder()],
  ]);
}

export function getPortfolioCodec(): Codec<PortfolioArgs, Portfolio> {
  return combineCodec(getPortfolioEncoder(), getPortfolioDecoder());
}

export function decodePortfolio<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Portfolio, TAddress>;
export function decodePortfolio<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Portfolio, TAddress>;
export function decodePortfolio<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Portfolio, TAddress> | MaybeAccount<Portfolio, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPortfolioDecoder()
  );
}

export async function fetchPortfolio<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Portfolio, TAddress>> {
  const maybeAccount = await fetchMaybePortfolio(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePortfolio<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Portfolio, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePortfolio(maybeAccount);
}

export async function fetchAllPortfolio(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Portfolio>[]> {
  const maybeAccounts = await fetchAllMaybePortfolio(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePortfolio(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Portfolio>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePortfolio(maybeAccount));
}
